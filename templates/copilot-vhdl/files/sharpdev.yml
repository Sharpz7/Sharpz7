version: 1

scripts:
  test: |
    # cd = change directory
    cd ./test
    ghdl -a test.vhd
    ghdl -a test_TB.vhd
    ghdl -e andorinvertTB
    ghdl -r andorinvertTB --wave="wave.ghw"

  # Add your commands here as you expand the code
  run: |
    cd ./src
    ghdl -a start.vhd

