version: 1

scripts:
  test: |
    ghdl -a test/test.vhd
    ghdl -a test/test_TB.vhd
    ghdl -e andorinvertTB
    ghdl -r andorinvertTB --wave="test/test.ghw"

  # Add your commands here as you expand the code
  run: |
    ghdl -a src/start.vhd

